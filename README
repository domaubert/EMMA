How to get:
	sudo apt-get install git
	git init
	git clone https://github.com/domaubert/Quartz.git
	cd Quartz

how to build:
	sudo apt-get install libopenmpi-dev gfortran
	make

how to launch
	on first launch:
		- create a "data" directory 
		- create a "data/movie" directory 
		- create a "bkp" directory 
	
	./genIC 

	mpirun -np N ./quartzcpu params/test_cosmo.run

